**** THIS IS A UTILITY FOR VIEWING THE INPUT FILE GENERATED BY THE INPUT GENERATOR ****

(1) To compile and generate an executable for the input viewer, type the following (without quotes) in the terminal:

`make`

(2) After step 1, you will have an executable called `inputViewer` in this folder. 

(3) To view an input file named `n100records.in` (for example) generated by `inputGen`, type in the following (without quotes) in the terminal:  

`./inputViewer -i n100records.in`

It will show the records (one per line) in the terminal. 

Each record is of the following form: 

Key: <int> value: 24 <int>s
 
	
